# Keep local and CI builds inside common runner limits. The previous 8G heap
# caused the Gradle daemon to be OOM-killed on 2-4 GB machines before compile.
org.gradle.jvmargs=-Xmx1536m -XX:MaxMetaspaceSize=512m -XX:ReservedCodeCacheSize=256m -XX:+HeapDumpOnOutOfMemoryError
org.gradle.workers.max=1
org.gradle.parallel=false
org.gradle.daemon=false
kotlin.daemon.jvmargs=-Xmx512m
android.useAndroidX=true
android.enableJetifier=true
android.newDsl=false
android.builtInKotlin=false
